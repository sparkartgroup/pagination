/*global describe, expect, it */
define(["pagination/resource"], function() {
	describe( "$.fn.paginate()", function () {

		/**
		 * Basic unit tests
		 */

		it( "should be defined", function() {
			expect( function(){} ).to.be.a( "function" );
		});

	});

});
